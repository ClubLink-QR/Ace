# Firebase で同期する（確実に動く方法・5分）

無料・クレジットカード不要・インストール不要。
ブラウザ操作だけで終わります。GitHub Pages に置いたままで動きます。

---

## なぜこれが必要か

GitHub Pages はファイルを配るだけで、データを保存する機能がありません。
別々の端末がデータを共有するには、どこかに保存場所が必要です。
Firebase はその保存場所を無料で貸してくれるサービスで、
**ブラウザから直接使えるように作られている**ため、確実に動きます。

---

## 手順

### 1. プロジェクトを作る

1. https://console.firebase.google.com/ を開く（Googleアカウントでログイン）
2. 「プロジェクトを追加」
3. 名前は何でもOK（例：`mystore`）→ 続行
4. Googleアナリティクスは **オフ** にして「プロジェクトを作成」

### 2. データベースを作る

1. 左メニュー「構築」→「Realtime Database」
2. 「データベースを作成」
3. ロケーションは **asia-southeast1（シンガポール）** を選ぶ（日本から一番近い）
4. セキュリティルールは **「テストモードで開始」** を選ぶ → 有効にする

### 3. URLを控える

データベースの画面上部にこういうURLが出ています。

```
https://mystore-default-rtdb.asia-southeast1.firebasedatabase.app/
```

これの**末尾に `store.json` を付けた**ものが、アプリに入れるURLです。

```
https://mystore-default-rtdb.asia-southeast1.firebasedatabase.app/store.json
```

### 4. アプリに設定する

各タブレットでアプリを開き

**設定 →「連携とデータ」→「Firebase を使う」** の欄にそのURLを貼り付け
→「**接続テスト**」を押す

「✓ 接続できました」と出れば完了です。
**14台すべてに同じURLを入れてください。**

---

## 重要：30日後に止まります

テストモードは30日で書き込みが禁止されます。運用前に必ず変更してください。

1. Realtime Database の「ルール」タブを開く
2. 中身を次に置き換えて「公開」

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

これで期限がなくなります。

### セキュリティについて

上のルールは「URLを知っている人は誰でも読み書きできる」状態です。
売上やお客様名を扱うので、URLは外部に漏らさないでください。

より安全にしたい場合は、合言葉を知っている人だけ通す設定にできます。
ルールを次のようにして、

```json
{
  "rules": {
    "store": {
      ".read": "auth != null || query.auth == 'あなたの合言葉'",
      ".write": "auth != null || query.auth == 'あなたの合言葉'"
    }
  }
}
```

アプリに入れるURLを `.../store.json?auth=あなたの合言葉` にします。

---

## うまくいかないとき

「接続テスト」を押すと理由が表示されます。

| 表示 | 原因と対処 |
|---|---|
| 書き込みを拒否されました | ルールが `false` のまま。手順どおり `.read` `.write` を `true` に |
| サーバーが 404 を返しました | URLの末尾に `store.json` が付いているか確認 |
| ブラウザから接続できません | URLの綴りミス、またはネット未接続 |

---

## 動作の速さ

Firebase を使うと、数秒おきに問い合わせるのではなく
**変更があった瞬間に相手から届く**方式（リアルタイム通信）になります。
注文を入れた瞬間に厨房の画面へ出ます。
